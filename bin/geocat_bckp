#!/usr/bin/env python

from geopycat.GeocatBackup import GeocatBackup

backup_dir = "M:/Appl/DATA/PROD/kogis/igeb/geocat/Metadatenapplikation geocat.ch entwickeln und betreiben (463)/Software geocat.ch entwickeln/Betrieb Applikation geocat.ch/XML Backup/test"

if __name__ == "__main__":
    GeocatBackup(env="prod", backup_dir=backup_dir, catalogue=False, users=False, groups=False, 
                    subtemplates=False)